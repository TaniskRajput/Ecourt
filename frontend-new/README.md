New frontend scaffold for E-Court

Run locally:

```bash
cd frontend-new
npm install
npm run dev
```

Notes:
- This is a minimal Vite + React starting point. We'll expand components, routes, and styles here.
- The original React app is preserved and marked as disabled in `frontend-react.DISABLED`.
